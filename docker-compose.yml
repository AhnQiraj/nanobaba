services:
  nanobaba:
    build:
      context: .
      dockerfile: Dockerfile
    image: ahnqiraj/nanobanana:local
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
